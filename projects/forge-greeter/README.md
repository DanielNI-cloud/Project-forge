# Forge Greeter

Forge Greeter is a small interactive Python program that welcomes the user, asks about an idea and its motivation, and prompts for a first step.

Run it from the repository root with:

```powershell
python projects/forge-greeter/forge-greeter.py
```
